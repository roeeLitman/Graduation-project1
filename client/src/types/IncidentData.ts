export interface IncidentData {
    totalIncidents: number;  
    year: number;            
    organization: string;    
}

export interface EventData {
    totalEvents: number;   
    organization: string;  
    year: number;          
}